# Core
numpy>=1.24

# All standard library — no external deps required for data, indicators, risk, monitor, backtest, or CLI
# signals.py uses urllib.request for OpenRouter calls (stdlib)
# data.py uses urllib.request, json, time, pathlib, os (stdlib)